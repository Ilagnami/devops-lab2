apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80           # внешний порт сервиса
      targetPort: 8080   # порт контейнера
      nodePort: 30036    # порт ноды
